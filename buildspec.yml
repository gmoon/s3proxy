version: 0.2
phases:
  install:
    commands:
      - apt-get update -y
      - make -j builddeps
  build:
    commands:
      - make -j tar
artifacts:
  files:
    - '*.tar.gz'
    - 'test-*'
  discard-paths: yes
  base-directory: target
