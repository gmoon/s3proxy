version: 0.2
phases:
  install:
    commands:
      - make debuginfo
      - make -j builddeps
      - make debuginfo
  build:
    commands:
      - make -j tar
      - make docker
artifacts:
  files:
    - '*.tar.gz'
    - 'test-*'
  discard-paths: yes
  base-directory: target
