version: 0.2
phases:
  install:
    commands:
      - make debuginfo
      - make -j builddeps
      - make debuginfo
  build:
    commands:
      - make -j tar
artifacts:
  files:
    - '*.tar.gz'
    - 'test-*'
  discard-paths: yes
  base-directory: target
