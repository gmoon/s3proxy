# The @forkzero scope lives on the public npm registry. Pin it here so a
# developer whose global ~/.npmrc maps @forkzero to GitHub Packages still
# installs @forkzero/s3-website-test-kit from public npm.
@forkzero:registry=https://registry.npmjs.org
